// interface UserModel extends mongoose.Model<UserDoc> {
//     build(attrs: UserAttrs): UserDoc;
//     // build(attrs: UserAttrs): any;
// }

/**
 * The parameters beetwen the angle brackets <> are generics; parameters that are not values or functions but types, pass to a function definition for type checks 
 * these arguments do not marry 1 to 1 with the normal function parameters, they can be used freely 
 */